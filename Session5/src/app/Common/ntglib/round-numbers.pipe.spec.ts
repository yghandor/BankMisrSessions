import { RoundNumbersPipe } from './round-numbers.pipe';

describe('RoundNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
