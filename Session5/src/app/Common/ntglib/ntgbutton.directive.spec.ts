import { NTGButtonDirective } from './ntgbutton.directive';

describe('NTGButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NTGButtonDirective();
    expect(directive).toBeTruthy();
  });
});
