import { ColorButtonsDirective } from './color-buttons.directive';

describe('ColorButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
